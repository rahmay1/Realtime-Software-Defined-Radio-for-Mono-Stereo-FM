This is where all the .raw files containing I/Q samples should be placed

The Python scripts assume the default .raw file is called iq_samples.raw

The automatically-generated .png, .wav, .bin files will also be placed here
